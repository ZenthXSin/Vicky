---
name: EMPTY
description: Kotlin object singleton: kotlin.reflect.jvm.internal.impl.util.ModuleVisibilityHelper$EMPTY. Auto-injected into script scope.
group: runtime-api
---

# EMPTY

Kotlin object (singleton). In scripts, use directly as `EMPTY`.

Full class: `kotlin.reflect.jvm.internal.impl.util.ModuleVisibilityHelper$EMPTY`

## Fields
- `INSTANCE`: EMPTY (static)

## Methods
- `isInFriendModule(arg0: DeclarationDescriptor, arg1: DeclarationDescriptor): boolean`

## Usage in TypeScript
```typescript
// Direct access — no constructor needed
var instance = EMPTY;
var result = EMPTY.isInFriendModule(..., ...);
```

---
name: OpenAiToolSchemaBuilder
description: Kotlin object singleton: org.example.vicky.agent.OpenAiToolSchemaBuilder. Auto-injected into script scope.
group: runtime-api
---

# OpenAiToolSchemaBuilder

Kotlin object (singleton). In scripts, use directly as `OpenAiToolSchemaBuilder`.

Full class: `org.example.vicky.agent.OpenAiToolSchemaBuilder`

## Fields
- `INSTANCE`: OpenAiToolSchemaBuilder (static)

## Methods
- `build(arg0: ToolRegistry): List`

## Usage in TypeScript
```typescript
// Direct access — no constructor needed
var instance = OpenAiToolSchemaBuilder;
var result = OpenAiToolSchemaBuilder.build(...);
```
